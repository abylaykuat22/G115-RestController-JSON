spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/G115-rest-db
    username: postgres
    password: postgres
  flyway:
    url: jdbc:postgresql://localhost:5432/G115-rest-db
    user: postgres
    password: postgres
    locations: classpath:db/changelog
